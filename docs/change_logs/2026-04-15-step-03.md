# 2026-04-15 Step 03

## 本步目标
在主流程测试中补强 carry 粘滞行为验证，并修正断言口径与当前架构的一致性。

## 已完成改动
1. 更新 tb/tb_mac16.v
- 新增 mode 切换后 carry 清零检查。
- 新增 Mode1 高负载序列（7FFF x 7FFF 连续输入）以触发溢出。
- 新增 sticky carry 断言：
  - 溢出后 carry 必须为 1。
  - 后续非溢出帧计算后 carry 仍需保持 1。

2. 修正断言策略
- 移除“按输出帧逐帧比对 carry”的检查。
- 原因：当前架构中 carry 是全局粘滞状态，与输出串行帧并非一一同步采样关系。

## 验证结果
- 命令: iverilog -g2012 -Wall -o /tmp/sim_mac16.vvp -s tb_mac16 rtl/*.v tb/tb_mac16.v && vvp /tmp/sim_mac16.vvp
- 结果: PASS

## 结论
- 主测试平台已覆盖：
  - Mode0 结果逻辑
  - Mode1 累加逻辑
  - mode 切换清空
  - 5 拍启动约束
  - sticky carry 保持约束

## 下一步计划
1. 增加连续帧高吞吐场景（用于评估现有 scheduler 的吞吐上界）。
2. 生成 Step 04 说明文件，记录吞吐能力与已知限制。
