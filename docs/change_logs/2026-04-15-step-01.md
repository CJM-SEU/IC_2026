# 2026-04-15 Step 01

## 本步目标
将子模块测试平台从日志型输出改为断言型自检，并开始固定产出修改说明文件。

## 已完成改动
1. 重写 tb/tb_serial_to_parallel.v
- 使用 1GHz 时钟。
- 使用边沿对齐的串行激励（在负边沿驱动，在正边沿采样）。
- 增加 data_valid/in_done 同拍一致性检查。
- 增加输出值与期望值逐帧比对。
- 增加未消费样本计数检查。

2. 重写 tb/tb_parallel_to_serial.v
- 使用 1GHz 时钟。
- 增加多帧数据发送和逐帧比对。
- 检查 out_ready 输出窗口固定为 24 拍。
- 检查空闲时 serial_out 必须为 0。
- 检查 out_done 只在输出窗口内出现。

3. 新增变更记录目录
- docs/change_logs/

## 影响范围
- 仅影响测试平台，不改变 RTL 功能逻辑。

## 下一步计划
1. 编译并运行三个 testbench（tb_mac16, tb_serial_to_parallel, tb_parallel_to_serial）。
2. 若断言失败，按失败点修复 RTL 或测试时序对齐。
3. 生成 Step 02 验证说明文件。
