# 2026-04-15 Step 02

## 本步目标
验证 Step 01 的测试平台改造是否稳定可用。

## 执行命令与结果
1. tb_mac16
- 命令: iverilog -g2012 -Wall -o /tmp/sim_mac16.vvp -s tb_mac16 rtl/*.v tb/tb_mac16.v && vvp /tmp/sim_mac16.vvp
- 结果: PASS

2. tb_serial_to_parallel
- 命令: iverilog -g2012 -Wall -o /tmp/sim_s2p.vvp -s tb_serial_to_parallel rtl/*.v tb/tb_serial_to_parallel.v && vvp /tmp/sim_s2p.vvp
- 结果: PASS

3. tb_parallel_to_serial
- 命令: iverilog -g2012 -Wall -o /tmp/sim_p2s.vvp -s tb_parallel_to_serial rtl/*.v tb/tb_parallel_to_serial.v && vvp /tmp/sim_p2s.vvp
- 结果: PASS

## 结论
- 三个核心 testbench 已全部通过。
- 当前可继续推进下一阶段：在现有断言基础上补充更多边界用例（符号边界、连续帧、carry 粘滞极限）。

## 下一步计划
1. 在 tb_mac16 增加 carry 粘滞专项强测（至少一次溢出后保持为 1，直到 reset 或 mode 切换）。
2. 增加连续帧压力测试，验证无间隔输入下时序稳定性。
3. 生成 Step 03 说明文件。
